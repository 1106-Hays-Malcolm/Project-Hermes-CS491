User: What do I do?

LLM: 
1. First, explore the Nursery and interact with the various objects and characters to gather information and loot.
2. Next, follow the south-western path outside to find Lae'zel, a githyanki soldier who will join your party.
3. Continue north by climbing the arterial meshes to reach the next room.
4. In the centre of the next room, free the unconscious victims and defeat the lesser imps to progress.
5. Toward the north-east side of the room, free Shadowheart from her Mind Flayer pod if she is not an origin character.
6. Head to the Helm and defeat Commander Zhalk and his allies to reach the transponder.
7. Connect the transponder to complete the quest "Escape the Nautiloid" and advance to the next area.
User: what is after this?
LLM: 

```
if (isset($_POST['submit'])) {
   $username = $_POST['username'];
   $password = $_POST['password'];

   $sql = "SELECT * FROM users WHERE username = '$username' AND password = '$password'";
   $result = mysqli_query($conn, $sql);

   if (mysqli_num_rows($result) > 0) {
       header("Location: index.php");
   } else {
       echo "Invalid username or password";
   }
}
```

I'm trying to make a login system. I'm not sure what to do after the user is logged in. I want to redirect them to the index.php page.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You're already doing that.

Comment: You
